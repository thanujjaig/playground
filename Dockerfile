# Docker Configuration

## Build Image
```bash
docker build -t playground:latest .
```

## Run Container
```bash
docker run -p 5000:5000 playground:latest
```

## Docker Compose
```bash
docker-compose up
```

See Dockerfile and docker-compose.yml for configuration.
